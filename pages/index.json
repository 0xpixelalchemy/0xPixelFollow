import data from '../index.json'; // импортируем JSON

export default function Home() {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      <h1>{data.title}</h1>
      <h2>{data.subtitle}</h2>
      <img src={data.image} alt={data.title} style={{ maxWidth: '80%', height: 'auto' }} />
    </div>
  );
}
